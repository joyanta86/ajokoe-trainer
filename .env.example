# Supabase project settings → API. Public (safe to expose to the browser);
# access to user data is enforced by Row Level Security, not by hiding these.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
